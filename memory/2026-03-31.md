# 2026-03-31 — Laverdi.tech Portal Deployment

## Session Summary

### Completed ✅
- **Supabase Migration:** Ran full schema (users, subscriptions, api_keys, usage_logs tables with RLS policies)
- **Stripe Setup:** 
  - Created webhook endpoint at `https://laverdi.tech/api/webhooks/stripe`
  - Obtained webhook secret: `whsec_REDACTED_STRIPE_WEBHOOK`
  - Configured test keys (publishable + secret)
- **SSH Access:** Generated ED25519 key pair, added to DigitalOcean, confirmed SSH works
- **VPS Portal Structure:** Created `/root/laverdi-portal` on VPS with:
  - `docker-compose.yml` (web + nginx services)
  - `.env` (all Supabase + Stripe credentials loaded)
  - `Dockerfile` (Node 18-alpine, npm install → build → start)
  - `package.json` (Next.js 14, React 18, Supabase, Stripe deps)
  - Config files: `next.config.js`, `tsconfig.json`, `tailwind.config.ts`, `postcss.config.js`
  - App structure: `pages/`, `components/`, `lib/`, `public/`, `styles/`
  - Landing page: `pages/index.tsx` (basic hero with Sign In link)
  - Global styles: `styles/globals.css` (Tailwind)
  - Nginx config: `nginx.conf` (reverse proxy to web:3000)
- **Firewall:** Opened UFW ports 80/tcp and 443/tcp
- **Docker Build:** Started `docker-compose up -d` (building Next.js app in container)

### In Progress 🔄
- Docker build/start (estimated 3-5 mins from 23:05 PDT)
- Waiting for containers to come online

### Next Steps
1. **Check Docker status:**
   ```bash
   docker ps
   docker logs laverdi-portal
   ```
2. **Verify port 80 is live:**
   ```bash
   curl http://localhost
   ```
3. **Point DNS:** Update laverdi.tech A record to 64.23.142.154
4. **SSL/HTTPS:** Set up Let's Encrypt cert (certbot in nginx container or add to compose)
5. **Full App Pages:** Copy remaining pages (auth, dashboard, billing, etc.) from workspace to VPS
6. **Test Stripe:** Trigger test subscription via portal
7. **Monitor logs:** `docker logs -f laverdi-portal`

### Credentials Summary
- **Supabase Project ID:** dcvrkpgvxqdcboostkpz
- **Supabase URL:** https://dcvrkpgvxqdcboostkpz.supabase.co
- **Stripe Keys:** Stored in `.env` on VPS
- **Webhook Secret:** whsec_REDACTED_STRIPE_WEBHOOK
- **VPS SSH:** `ssh -i ~/.ssh/id_ed25519 root@64.23.142.154`
- **Portal Path:** `/root/laverdi-portal`

### Notes
- SSH via password failed; switched to key-based auth
- SCP file copy hung; used console `cat >` heredocs instead (slow but reliable)
- Docker build initially failed on `npm ci` (no lockfile); switched to `npm install`
- Minimal portal structure created to get Docker running; full pages/API routes still need copying
