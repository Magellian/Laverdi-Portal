# 2026-03-28

## Discussions
- Chris sent a project plan for a production-grade Webhook-to-Exchange Trading Bridge to replace a brittle "AI desktop agent" concept.
- The plan outlines 4 phases: Exchange Setup, Webhook Receiver, TradingView Integration, and an Optional AI Filter Layer.
- Decision pending: Build via Python/FastAPI+CCXT (Infrastructure route) or an n8n workflow (No-code route).

## Progress
- Created `memory/project-trading-bridge/README.md` to track the architecture and execution plan.

---

## Crypto Trader - Mission Control Tab (Session 2)

### What We Built
- Added a **Crypto Trader tab** to Mission Control (`mission-control/index.html`)
  - Shows: Total Account Value, Available Cash, 24h PnL, Bot Status, Active Positions table, Bot Execution Logs
  - Auto-refreshes every 10s by polling `mission-control/state/crypto.json`
- Built `mission-control/bots/kraken_bot.py`
  - Connects to Kraken via ccxt using keys in `src/trading-bridge/.env`
  - Polls every 15s, writes live data to `state/crypto.json`
  - Handles Kraken asset name remapping (XXBT→BTC, XETH→ETH, etc.)
  - Uses 24h open price as proxy for avg entry / PnL calc
  - Session-based PnL delta tracked from first run

### Current Account State (live as of 08:48 PDT)
- Account Value: $200.00 (all cash, no open positions)
- Exchange: Kraken
- Bot Status: Live and syncing

### Keys Location
- Kraken API keys: `src/trading-bridge/.env` (KRAKEN_API_KEY, KRAKEN_API_SECRET)
- Same file has ANTHROPIC_API_KEY for the AI risk manager

### Existing Infrastructure (already built, in src/trading-bridge/main.py)
- FastAPI webhook receiver on port 8000
- TradingView webhook → Kraken execution bridge
- Claude Haiku AI risk manager gate (reads crypto news, APPROVE/DENY trades)
- DRY_RUN = True safety switch
- ccxt Kraken integration already working

### Next Steps (discussed, not started)
1. Connect bot + webhook — kraken_bot.py watches account, main.py fires trades
2. Build a strategy — SMA crossover signals in TradingView → webhook alerts
3. Test in DRY_RUN mode first, then flip to live

### Cleanup Done
- Removed coinbase_bot.py and coinbase .env (we are Kraken-only)

---

## Session 3 — 2026-03-28 09:00–10:09 PDT

### Decisions
- **No TradingView** — skipping subscription fee, using Kraken-native OHLCV data for signals
- **DRY_RUN flipped to FALSE** — system now executes real trades

### What We Built
- Flipped `DRY_RUN = False` in `src/trading-bridge/main.py`
- Started trading bridge on port 8000 (background process `plaid-pine`)
- Built `src/trading-bridge/signal_engine.py`
  - Pulls 5m OHLCV candles from Kraken via ccxt
  - Calculates SMA9 and SMA21
  - Fires BUY on fast>slow crossover, SELL on fast<slow crossover
  - Won't re-fire the same direction twice (dedup logic)
  - Posts to webhook at http://127.0.0.1:8000/webhook with passphrase
  - Polls every 60 seconds
- Started signal engine (background process `tide-glade`)
- Created `mission-control/serve.ps1` — serves dashboard via Python HTTP server on port 8080

### First Live Trade
- 09:29 PDT: SMA9 (66,996) crossed above SMA21 (66,939) → BUY signal
- AI risk manager approved
- **LIVE order executed: 0.0001 BTC/USD (~$6.70)**
- As of 10:09 PDT: still holding BUY, SMA9 still above SMA21, no SELL signal yet

### Dashboard Fix
- Root cause of blank Crypto tab: `showTab()` was using `display: grid` on the crypto section
- Fixed: crypto tab now uses `display: block` and triggers `fetchCryptoData()` on tab click
- HTTP server confirmed serving `crypto.json` correctly at localhost:8080

### Running Processes (as of session end)
| Process | Description | Session ID |
|---------|-------------|------------|
| `plaid-pine` | Trading bridge (FastAPI, port 8000) | Background |
| `tide-glade` | Signal engine (SMA9/21, BTC/USD 5m) | Background |
| `young-nexus` | Mission Control HTTP server (port 8080) | Background |
| Kraken bot | Account sync every 15s → crypto.json | Running separately |

### Key Files
| File | Purpose |
|------|---------|
| `src/trading-bridge/main.py` | Webhook receiver + AI risk gate + Kraken executor |
| `src/trading-bridge/signal_engine.py` | Kraken-native SMA crossover signal generator |
| `mission-control/bots/kraken_bot.py` | Account state sync bot |
| `mission-control/state/crypto.json` | Live state (written by bot, read by dashboard) |
| `mission-control/index.html` | Dashboard UI |
| `mission-control/serve.ps1` | Start HTTP server for dashboard |
| `src/trading-bridge/.env` | API keys (Kraken + Anthropic) |

## Session 4 — 2026-03-28 10:33–00:02 PDT (Next Day)

### API Testing
- Verified OpenAI gpt-5.4 is online and working ✅
- Verified Google Gemini 3.1 Pro is online and working ✅
- Verified Anthropic Claude Opus is online and working ✅
- Set fallback chain: OpenAI (primary) → Google → Anthropic

### Model Fallback Chain Decision
- Primary model: `openai-codex/gpt-5.4`
- Fallback when Google caps: `google/gemini-3.1-pro-preview`
- Then fallback to: `anthropic/claude-opus-4-6`
- Updated MEMORY.md to reflect new chain

### Trading Bridge Status
- Trading bridge still running and live
- Account Value: $199.93 (down $0.07 from earlier session)
- 0 open positions (BTC buy from 09:29 PDT was closed with SELL signal)
- Bot syncing every 15s to `mission-control/state/crypto.json`
- Signal engine polling every 60s for SMA9/SMA21 crossovers
- Strategy: **Staying with BTC/USD** (no pivot to volatile altcoins)

### Mission Control Dashboard
- HTTP server restarted on port 8080 — was stopped, brought it back up
- Crypto Trader tab confirmed syncing live with bot data
- Dashboard accessible at `http://localhost:8080` (or via internal IP if on Moonlight remote)

### Docker/n8n Status
- Docker Desktop not found in standard location — needs investigation
- WSL docker-desktop is running but Docker daemon not responding to commands
- Etsy POD n8n workflow pending — unable to start until Docker is resolved

## Follow-ups
- Resolve Docker Desktop installation/startup issue to get n8n running for Etsy workflow
- Monitor next SMA signal on BTC/USD
- Next: add stop-loss logic to signal engine
- Next: add trade log to crypto.json so dashboard shows entry price + P&L per position
