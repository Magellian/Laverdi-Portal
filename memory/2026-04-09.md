# 2026-04-09

## Discussions
- Diagnosed an `openclaw doctor` Gateway lockup (stale ghost Node process holding port 18789) preventing updates.
- Booted up the Trading Bridge (FastAPI `main.py`) and Local Signal Engine (`signal_engine.py`).
- Switched the AI Risk Gate from Anthropic (due to exhausted credits) to Google Gemini, with a robust fallback to OpenAI ChatGPT in case of transient API errors (like 503s).
- Discussed and architected a Futures trading branch alongside the existing Spot system.

## Decisions
- Used `ccxt.krakenfutures` for the new futures branch to cleanly separate Spot and Futures logic.
- Upgraded the `signal_engine.py` with Stop-And-Reverse (SAR) logic to automatically close and flip positions using 2x size multipliers when market sentiment shifts.
- Implemented a safety toggle (`ENABLE_FUTURES=False`) in `.env` to prevent accidental futures trades until keys are verified.
- Live mode (`DRY_RUN = False`) is activated for the primary engine.

## Progress
- Trading Bridge is now fully capable of routing both Spot and Futures trades based on the webhook payload `{"market": "spot"|"futures"}`.
- Added AI Risk Manager transient error retry logic (retries Gemini 3 times on 429/500/503 before falling back to OpenAI).
- Validated signal engine triggers and webhook routing end-to-end.

## Follow-ups
- Kraken Futures API keys are now present in `src/trading-bridge/.env` and the bridge confirms `futures_connected: true`.
- `ENABLE_FUTURES` remains `False`, so futures execution is still intentionally blocked until Chris decides to arm it.
- Spot bridge sizing was reduced from 30% to 20% of available cash (`RISK_PERCENTAGE = 0.20`).
- Bridge is live (`DRY_RUN = False`), signal engine is running, and the AI gate order is Gemini primary with OpenAI fallback.
