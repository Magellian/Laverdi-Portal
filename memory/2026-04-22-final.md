# Session Final State - 2026-04-22 19:23 PDT

## 🎯 WHERE WE ARE (End of Day)

### FULLY WORKING ✅
1. **Portal** — https://app.laverdi.tech (HTTPS, mobile)
2. **Dashboard** — Shows tier, API key, "Launch OpenClaw Agent" button
3. **Agent Access** — Button → https://agent.laverdi.tech/?token=TOKEN → OpenClaw UI loads
4. **Device Pairing** — Auto-approve loop running inside container (10s poll)
5. **Inference Pipeline** — DO Inference calling Llama 3.3 70B, tools execute, model responds
6. **Persistent Storage** — Docker volume `openclaw-e64c80d4` preserves pairings across restarts
7. **SSL** — Let's Encrypt on app.laverdi.tech + agent.laverdi.tech (expires 2026-07-21)

### CURRENT CONTAINER STATE
- Container: `openclaw-e64c80d4-daea-4b6e-8df9-60ef2f476b0c`
- Volume: `openclaw-e64c80d4` (persistent)
- Port: 9000 → 18789
- Model: `do-inference/llama3.3-70b-instruct` (openai-completions API)
- Token in DB: `130923167e489367a3dd95ef39b81e8835e2d02e3c152b9a`
- Entrypoint: `/tmp/openclaw-entrypoint.sh` (MUST BE PRESENT ON VPS RESTART)

### KNOWN ISSUES
- **Rate limits** — DO Inference free tier hits limits quickly with Llama 70B. Consider smaller model or upgrade DO plan.
- **Bootstrap loop** — Fresh workspace + BOOTSTRAP.md = agent gets stuck. Fix: delete BOOTSTRAP.md, clear sessions.json
- **Token refresh problem** — Every container restart generates a new auth token. Must update DB manually (or automate).
- **Model access** — DO Inference key only covers DO-native models (Llama, etc.). Anthropic/OpenAI models require billing upgrade on DO.
- **Entrypoint file** — `/tmp/openclaw-entrypoint.sh` is lost on VPS reboot. Should move to `/root/openclaw-entrypoint.sh`.

### MODELS AVAILABLE ON DO INFERENCE (confirmed working)
- `llama3.3-70b-instruct` ✅ (rate limited on free tier)
- `openai-gpt-4o-mini` (untested, may work)
- `anthropic-claude-4.6-sonnet` ❌ (requires paid tier)

### IMMEDIATE NEXT STEPS
1. **Move entrypoint to permanent location**: `cp /tmp/openclaw-entrypoint.sh /root/openclaw-entrypoint.sh`
2. **Automate token sync** — On container start, read token from config and update Supabase automatically
3. **Rate limit** — Try smaller model (`openai-gpt-4o-mini`) or upgrade DO inference plan
4. **Multi-user provisioning** — Each user needs their own container + port + volume + token in DB
5. **Stripe webhook → auto-provision** — Payment triggers full container spin-up
6. **Pre-delete BOOTSTRAP.md** from workspace template in Docker image

### ARCHITECTURE (Current Working State)
```
User → https://app.laverdi.tech/dashboard
  → Clicks "Launch OpenClaw Agent"
  → https://agent.laverdi.tech/?token=<from DB>
  → nginx proxies to localhost:9000
  → OpenClaw container (Docker, persistent volume)
  → DO Inference API → Llama 3.3 70B
  → Response back to user in browser
```

### KEY FILES ON VPS
- Entrypoint: `/tmp/openclaw-entrypoint.sh` (⚠️ move to /root/)
- Portal code: `/root/laverdi-portal/`
- nginx config: `/etc/nginx/nginx.conf`
- SSL certs: `/etc/letsencrypt/live/app.laverdi.tech/`
- Docker volume: `openclaw-e64c80d4`

### KEY SCRIPTS (local workspace)
- `openclaw-entrypoint.sh` — Container startup, config, auto-approve loop
- `update-token.sh` — Updates Supabase instances table with current token
- `api-get-instance.ts` — Portal API: fetches instance token for logged-in user
- `dashboard-updated.tsx` — Current dashboard with Launch button
- `auto-approve-loop.sh` — Standalone auto-approve script

### TIER → MODEL MAPPING (target, not yet implemented per-user)
- Free → llama3.3-70b-instruct (or haiku equivalent)
- Starter ($99) → anthropic-claude-4.6-sonnet (needs DO billing upgrade)
- Professional ($249) → anthropic-claude-opus or GPT-5
