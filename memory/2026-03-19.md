# 2026-03-19

## Discussions
- Chris named the assistant Crawford.
- Established operating context: execution-first, direct, operator-style support.
- Captured strategic shift away from Laverdi Vending and toward an AI receptionist / automation agency.
- Immediate deployment focus: Retell AI receptionist for after-hours handling at Fife RV.
- Communication preference: concise, actionable, structured, no fluff.
- Chris asked to implement a daily journal memory system inspired by Alex's OpenClaw setup from a tutorial video.

## Decisions
- Use `memory/YYYY-MM-DD.md` as a running daily journal for important discussions, decisions, progress, and follow-ups.
- Keep `MEMORY.md` as the distilled long-term memory layer.

## Progress
- Added a formal Daily Journal Protocol to `AGENTS.md`.
- Created `memory/README.md` documenting the journal system.
- Created `memory/TEMPLATE.md` with a standard daily note format.
- Built a local `mission-control/` dashboard with calendar, memory, docs, 2D pixel office, and team visualization sections.
- Created `docs/mission-control-spec.md` and `docs/artifact-index.md` to anchor generated artifacts.
- Added a Mission Control v1 Primary Model switcher UI with grouped model options, quick picks, config preview, and dashboard-local saved selection as the first step toward live OpenClaw model switching.

## Follow-ups
- Keep appending notable discussions and decisions to today's daily journal instead of relying on unstated memory.
- Upgrade mission control to ingest real scheduled tasks and directory contents dynamically instead of seeded demo data.
- Run the first intentional Crawford Guard scan when ready so live findings populate the dashboard/docs.
- Expand Mission Control further if Chris wants live restart controls instead of runbook-only guidance.

## Progress
- Inspected the public npm metadata/readme for `@openguardrails/moltguard` without installing it.
- Built a local first-party plugin scaffold at `local-plugins/crawford-guard/` that mirrors the published capability surface: prompt-injection detection, secret sanitization, workspace scanning, dashboard, and command handlers.
- Documented the provenance and limits in `docs/crawford-guard-notes.md`.
w plugin with a proper manifest and plugin load path in `openclaw.json`.
- Verified the local Crawford Guard dashboard is reachable on `http://127.0.0.1:53679`.
- Added a dedicated Crawford Guard panel to Mission Control and created `docs/crawford-guard-findings.md` as the operational findings snapshot.
- Connected Anthropic, Gemini, and Groq provider credentials into OpenClaw and updated Mission Control to reflect the real provider/auth stack and active fallback chain.
- Chris asked to save the Discord workflow instruction set and bring it back on Saturday at 10:00 AM Pacific for review/implementation; scheduled reminder job `reminder:discord-workflow-review`.
- Chris wants two overnight brainstorm/work items queued up: (1) automated trading bot/workflow ideas, and (2) an AI receptionist implementation plan using Retell with an ElevenLabs wrapper, step by step.
- Chris provided a detailed build prompt for a `MISSION CONTROL — SUB-AGENT DEPLOYMENT MODULE` inside OpenClaw, centered on Crawford as permanent top-level coordinator with visual agent nodes, hierarchy/delegation, per-agent models, personalities, planned actions, and a node/canvas UI.
