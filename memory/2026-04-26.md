# 2026-04-26 Session Notes

## ✅ LAVERDI PORTAL + OPENCLAW HOSTING — FULL STACK OPERATIONAL

**Date:** Sunday, April 26, 2026

### What Was Accomplished Today

**OpenClaw Instance Provisioning — WORKING END-TO-END:**
1. ✅ User signs up at `https://laverdi.tech`
2. ✅ Admin upgrades user tier via `/api/admin/upgrade-user`
3. ✅ Provisioning creates Docker container on VPS
4. ✅ OpenClaw gateway starts inside container
5. ✅ Nginx reverse proxy routes `/agent/{port}/` → `127.0.0.1:{port}`
6. ✅ WebSocket connection works through nginx (WSS upgrade confirmed)
7. ✅ Control UI loads and connects — **user can chat with their agent**

**Current Running Instance:**
- Container: `openclaw-6f95cda6-1777243716714`
- Port: 9002
- URL: `https://laverdi.tech/agent/9002/`
- Token: `e6cc1dc00399443481102a0f2e6fb3a6b5cacbb1b44845b2aac8772ca891ebe3`
- Status: ✅ Connected, gateway responding, WebSocket stable

**Nginx Configuration Fixed:**
- Agent proxy pattern: `/agent/{port}/` → strips prefix, proxies to localhost
- WebSocket upgrade headers working
- HTTP/2 + SSL through Let's Encrypt
- Regex capture: `~* ^/agent/(\d+)(/.*)?$` with `$agent_port` and `$agent_path`
- Assets, favicon, config JSON all loading correctly

**Portal Rebuild In Progress:**
- Dashboard "Open Web Interface" button being updated to link to correct URL with token
- Building via Docker on VPS (was still exporting layers at end of session)

### Architecture Summary
```
User Browser
  → https://laverdi.tech (Portal - Next.js on port 3000)
  → https://laverdi.tech/agent/9002/ (OpenClaw Control UI)
      → wss://laverdi.tech/agent/9002/ (WebSocket to gateway)
          → nginx reverse proxy
              → ws://127.0.0.1:9002/ (Docker container)
```

### Infrastructure
- **VPS:** DigitalOcean droplet `64.23.142.154`
- **Domain:** `laverdi.tech` (Gandi.net)
- **SSL:** Let's Encrypt + auto-renewal
- **Portal:** Next.js 14 in Docker
- **Command Center:** Python Flask API for provisioning
- **Database:** Supabase (user profiles, instances, tiers)
- **Payments:** Stripe (test mode, UI issue still unresolved)

### Known Issues
- Stripe checkout "page not found" in test mode (backend works, UI doesn't)
- Port 9000 container doesn't exist (old references in nginx logs causing 111 errors)
- SendGrid email: DNS verification still pending (noreply@laverdi.tech)
- OpenClaw version on container is v2026.4.21, update available (v2026.4.24)
- Codex ACP probe fails inside container (ENOENT — not critical)
- Gateway logs: "Proxy headers detected from untrusted address" — need to configure `gateway.trustedProxies`

### DO Inference API
- **Endpoint:** `https://inference.do-ai.run/v1` (OpenAI-compatible)
- **Key:** `sk-do-REDACTED_DO_INFERENCE_KEY`
- **Available models (our key):** Only open-source — `llama3.3-70b-instruct`, `deepseek-r1-distill-llama-70b`
- **Blocked:** All Anthropic + OpenAI models (need higher DO tier)
- **Tool calling test:** Llama 3.3 = ✅ fast (1.6s), DeepSeek R1 = ❌ no tools

### Tier System (FINAL)
```
Free/Trial  → Haiku 4.5 (50K/day)  → Llama 3.3 fallback (DO inference)
Starter     → Sonnet 4.6 (500K/day) → Haiku → Llama 3.3
Pro         → Opus 4.6 (2M/day)     → Sonnet → Haiku → Llama 3.3
```
- Anthropic API key = single key for all tiers (usage-based billing)
- DO inference = free-tier fallback only (open-source models)
- Users can `/model` down to save tokens
- At cap, graceful degradation to next tier's model

### Production Droplet Provisioning
- **New droplet:** `laverdi-prod-001` (ID: 567454297)
- **IP:** `64.23.253.97`
- **Spec:** `s-4vcpu-8gb-intel` — 4 CPU, 8GB RAM, 160GB SSD, $56/mo
- **Region:** SFO3
- **Status:** Setting up Docker, nginx, SSL, Command Center
- **Persistent volumes:** `/var/lib/laverdi/users/{user_id}/` mounted per container
- **DO account limits:** 3 droplets max, shared CPU only (dedicated needs limit increase request)

### Next Steps
1. Complete prod server setup (Docker, OpenClaw image, Command Center, nginx, SSL)
2. Point laverdi.tech DNS to new IP (64.23.253.97)
3. Configure `gateway.trustedProxies` in container OpenClaw config
4. Request DO account limit increase for dedicated CPU
5. SendGrid DNS verification for email delivery
6. Test full user signup → upgrade → provision → connect flow
7. Stripe test mode investigation

---

## 🔄 LATE SESSION — NEW PROD SERVER MIGRATION (7:30 PM PT)

### Portal Migrated to New Production Server (64.23.253.97)
- ✅ Portal code tarred from old server (64.23.142.154) → transferred to new server
- ✅ `.env.local` copied and updated: `VPS_API_URL=http://127.0.0.1:8000` (localhost since Command Center is on same host)
- ✅ Docker build completed successfully on new server (`laverdi-portal:latest`)
- ✅ Portal container running: `docker run -d --name laverdi-portal -p 3000:3000`
- ✅ `https://laverdi.tech` returns **200 OK** — portal is live on new server
- ✅ DNS already pointed to 64.23.253.97, SSL working

### Provisioning Test on New Server
- ✅ Command Center responding: `curl http://127.0.0.1:8000/health` → `{"docker":"ok","status":"ok"}`
- ✅ Provision endpoint works: `POST /api/provision-container` with proper JSON body
- ✅ Test container created: `openclaw-test-pro-1777257114` on port 9000
- ✅ Container got model config: Haiku 4.5 primary, Llama 3.3 fallback, 50K token cap
- ⚠️ Gateway config race condition: Config overwrite happened during startup, gateway may have stalled
- Container was being restarted when session compacted

### Running Containers on New Server
```
laverdi-portal          — Port 3000 (Next.js portal)
laverdi-command-center  — Port 8000 (Flask provisioning API)
openclaw-test-pro-*     — Port 9000 (test OpenClaw instance)
```

### PowerShell SSH Quoting Issue
- JSON bodies with quotes get mangled through PowerShell → SSH → curl chain
- **Workaround:** Write JSON to a local file, SCP it to server, then `curl -d @/tmp/file.json`

### Status at Session End
- New prod server (64.23.253.97) is the active production host
- Portal live, Command Center healthy, provisioning working
- Need to verify test container gateway actually started cleanly after restart
- Old server (64.23.142.154) still has containers but DNS no longer points there
