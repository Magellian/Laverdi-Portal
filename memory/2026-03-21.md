# 2026-03-21

## Decisions
- Saved OpenClaw model configuration to long-term memory.
- Kept `openai-codex/gpt-5.4` as primary model.
- Set fallback order to: `anthropic/claude-opus-4-6`, `google/gemini-3-flash-preview`, `xai/grok-code-fast-1`, `openrouter/hunter-alpha`, `ollama/gpt-oss:latest`, `ollama/glm-4.7-flash:latest`.

## Progress
- Connected Anthropic, Gemini, and xAI/Grok as selectable models in OpenClaw.
- Cleaned up stale `models.json` temp-file issue blocking registry updates on Windows.
- Verified gateway health after restart.
- Located and confirmed current watchdog/restart scripts in `C:\Users\chris\.openclaw\workspace\scripts\`:
  - `openclaw-watchdog.ps1`
  - `openclaw-safe-restart.ps1`
- Added hidden VBScript launchers:
  - `C:\Users\chris\.openclaw\workspace\scripts\openclaw-watchdog-hidden.vbs`
  - `C:\Users\chris\.openclaw\workspace\scripts\openclaw-safe-restart-hidden.vbs`
- Updated both PowerShell scripts to start the gateway with `-WindowStyle Hidden` instead of `Minimized` to reduce visible flashing.
- Rewired the scheduled task `OpenClaw Watchdog` to run invisibly via:
  - `wscript.exe "C:\Users\chris\.openclaw\workspace\scripts\openclaw-watchdog-hidden.vbs"`

## Notes
- The screen flash was likely caused by Task Scheduler launching `powershell.exe` visibly each minute, not just by script internals.
- Preferred hidden launch path going forward:
  - scheduled task -> `wscript.exe` -> hidden `.vbs` -> `powershell.exe -WindowStyle Hidden` -> `.ps1`
- If future restart/launcher behavior needs auditing, start with the scheduled task action plus these four script files.

## Mission Control / Sub-Agent Work
- Audited the Mission Control topology and confirmed the empty/weak live view was a mix of prototype limitations plus at least one real JS issue (`applyStateObject` missing in the earlier flow).
- Patched `mission-control/app.js` so the dashboard now has:
  - embedded fallback state for safer local opening
  - runtime snapshot refresh controls
  - freshness/status indicators
  - inspector session metadata
  - local branch controls (spawn template, pause, retry, escalate)
- Updated Mission Control routing/model policy to match current preferred stack:
  - primary: `openai-codex/gpt-5.4`
  - fallback chain: `anthropic/claude-opus-4-6`, `google/gemini-3-flash-preview`, `xai/grok-code-fast-1`, `openrouter/hunter-alpha`, `ollama/gpt-oss:latest`, `ollama/glm-4.7-flash:latest`
- Expanded planned topology/lanes so Mission Control now shows a more complete operating structure:
  - Crawford / coordinator
  - planning
  - research
  - build
  - automation
  - security / guard
  - delivery
- Added first-pass runtime mapping so the dashboard can reflect the live main session from `mission-control/state/runtime-sessions.json` and show unmatched runtime sessions as live runtime nodes.
- Added first real bridge primitives for dashboard-to-runtime orchestration:
  - spawned dashboard branches now create persisted `pendingSpawnIntents`
  - each spawned branch gets a unique `binding` key
  - runtime overlay attempts to bind live sessions back onto the intended planned node using that binding key
- Current state of the project:
  - Mission Control looks and behaves much more like a control surface
  - it is still not yet directly executing real OpenClaw spawns from the dashboard itself
  - the next implementation step is an executor layer that consumes spawn intents and launches real sessions/subagents, then writes session bindings back into Mission Control state
- Chris asked to save this thread so future sessions know exactly where the Mission Control/runtime-bridge work stands.
