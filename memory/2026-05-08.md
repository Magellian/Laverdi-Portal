# 2026-05-08 — MVP COMPLETE: LAVERDI PROVISIONING PIPELINE WORKING END-TO-END

## 🎉 STATUS: MVP WORKING

Full pipeline verified: User signup → Vultr instance → Docker + OpenClaw → Dashboard shows "Online" with IP/port/connect button.

---

## What Was Fixed Today

### Root Cause #1: Missing `model_id` field (THE BIG ONE)
- **Problem:** `provision.ts` Supabase insert was silently failing
- **Error:** `null value in column "model_id" of relation "instances" violates not-null constraint`
- **Fix:** Added `model_id: 'haiku-4.5'` and `port: 9000` to the insert
- **Impact:** This blocked provisioning for 3+ days. One missing field.

### Root Cause #2: nginx.conf had stale port 3000 references
- **Problem:** `sites-enabled/default` said port 3005 but `nginx.conf` had 5 references to port 3000
- **Fix:** `sed -i 's/proxy_pass http:\/\/127.0.0.1:3000/proxy_pass http:\/\/127.0.0.1:3005/g' /etc/nginx/nginx.conf`

### Root Cause #3: Webhook container_id mismatch
- **Problem:** Cloud-init sends `inst-TIMESTAMP-RANDOM` but provision API stores Vultr UUID
- **Status:** Manually fixed for test. Needs code fix to align IDs.

### Trading Bridge Restarted
- Was not running (port 8000 conflict)
- Killed stale process, restarted successfully
- All signals showing HOLD (BTC/SOL/SUI)
- No trades since April 29 (market conditions, not a bug)

### Workspace Cleanup
- **Before:** 577 files, 128MB of chaos
- **After:** 15 essential files + `_archive/` (655 files, recoverable)
- Committed as clean workspace

---

## Remaining Fixes (Not Blockers — Polish)

### 1. Double-provision guard
- Signup fires provision API twice (race condition or double-click)
- Fix: Add `IF NOT EXISTS` check or debounce in signup handler

### 2. Cloud-init webhook auto-callback
- `scripts-user` module crashes, preventing automatic webhook call
- Fix: Debug the cloud-init script — likely the `curl` webhook call syntax
- The Vultr metadata URL might also be wrong (was DO format)

### 3. Container ID alignment
- Provision API stores Vultr UUID as `container_id`
- Cloud-init sends `inst-TIMESTAMP` as `instanceId`
- Webhook can't match them
- Fix: Store `inst-` ID in DB, OR pass Vultr UUID to cloud-init

### 4. Systemd service for npm
- npm keeps crashing and not restarting
- Fix: Create `/etc/systemd/system/laverdi-portal.service`
- Auto-restart on failure

### 5. Delete duplicate Vultr instance
- Two instances created for same user (double-provision)
- Delete one to save $2.50/mo

---

## Current Server State

### Portal Server (66.42.70.66)
- nginx: Running, proxying to port 3005
- npm/Next.js: Running on port 3005 (needs systemd service)
- All env vars loaded from .env.local

### Test Instance (137.220.36.137)
- OpenClaw: Running in Docker, port 9000
- Model: openai/gpt-5.5
- Status: "ready" in database
- Dashboard: Shows "Online" with IP/port

### Second Instance (144.202.83.13) — DUPLICATE, DELETE
- Also running OpenClaw
- Created by double-provision bug

---

## Key Commands Reference

### Restart portal
```bash
pkill -9 -f next; sleep 2; cd /root/laverdi-portal && PORT=3005 npm start
```

### Check instances table
```bash
cd /root/laverdi-portal && node -e "const{createClient}=require('@supabase/supabase-js');const s=createClient('https://dcvrkpgvxqdcboostkpz.supabase.co','eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRjdnJrcGd2eHFkY2Jvb3N0a3B6Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3NTAwNjI4MiwiZXhwIjoyMDkwNTgyMjgyfQ.oS_T_nKibwBMf7Jfod2gMNnnTu8FPk8kdhlDSk0onNY');s.from('instances').select('*').then(r=>{console.log(JSON.stringify(r.data,null,2));});"
```

### Manual webhook call (template)
```bash
curl -X POST http://localhost:3005/api/webhooks/instance-ready -H "Content-Type: application/json" -d '{"instanceId":"VULTR-UUID","userId":"USER-UUID","instanceIp":"IP","instancePort":9000,"token":"webhook-secret-token-change-in-production"}'
```

---

## Credentials (Current)
- Portal SSH: root@66.42.70.66 / F,6f$)bZKYr9CTDN
- Test instance SSH: root@137.220.36.137 / m+R2sE.bSkVb,7B6
- Vultr API: 7HX3W7CLSGH4VS27CQFHTKTN6TTAGDM4HUSA
- Portal running on port 3005 (nginx proxied)

---

## All 5 Fixes Applied (7:31 PM PDT)

1. ✅ Systemd service — `laverdi-portal.service` enabled, auto-restarts
2. ✅ Webhook matches on `user_id` instead of `container_id`
3. ✅ Double-provision removed — deleted duplicate `fetch('/api/provision')` in signup.tsx
4. ✅ Cloud-init fixed — removed `set -e`, fixed Vultr metadata URL, added logging
5. ✅ Duplicate Vultr instance deleted (144.202.83.13 / 3b45ade6)

**Remaining instance:** 137.220.36.137 (working, OpenClaw running)

## Full Product Loop Complete (2026-05-11 12:13 PM PDT)

**EVERYTHING WORKS END-TO-END:**
1. ✅ User signs up at https://laverdi.tech
2. ✅ Vultr instance auto-provisioned
3. ✅ Docker + OpenClaw boots via cloud-init
4. ✅ Dashboard shows "Online" with IP/port
5. ✅ Wildcard SSL: `*.agent.laverdi.tech` → portal proxy → instance
6. ✅ User connects via https://test.agent.laverdi.tech
7. ✅ Device pairing approved → **Connected to AI assistant**

**SSL Setup:**
- Wildcard cert: `/etc/letsencrypt/live/agent.laverdi.tech/` (expires 2026-08-07)
- DNS: `*.agent.laverdi.tech` → 66.42.70.66 (portal)
- Also `agent.laverdi.tech` → 66.42.70.66
- TXT: `_acme-challenge.agent` (for cert renewal)
- nginx.conf has agent proxy block BEFORE laverdi.tech block (order matters!)
- Portal proxies HTTPS → instance:18789
- `sites-enabled/default` was REMOVED (nginx.conf handles everything)

**Device Pairing:**
- `openclaw devices approve --latest` (must run quickly after browser connects)
- Need to automate this for production (auto-approve or pre-pair in cloud-init)

---

## Cloud-Init Automation Fixes (2026-05-11 3:30-4:00 PM PDT)

**Problem:** Cloud-init health check used `curl -sf http://localhost:9000/` which returns empty (OpenClaw requires auth), so script exited before injecting API key and calling webhook.

**Fixes applied to provision.ts:**
1. ✅ Health check now uses `docker logs openclaw 2>&1 | grep -q "gateway.*ready"` instead of HTTP curl
2. ✅ Script no longer exits on health check failure (WARNING instead of exit 1)
3. ✅ Sleep interval changed from 1s to 2s (less aggressive polling)
4. ✅ Duplicate provision guard added (checks instances table before creating)

**Still need (not yet automated in cloud-init):**
- API key pre-injection (currently done manually via docker exec after boot)
- Allowed origins for wildcard SSL (currently done manually)
- Webhook callback (should work now with health check fix — NEEDS TESTING)
- Dynamic nginx routing (hardcoded to one IP — need DB lookup)
- Device pairing auto-approval

**Current cloud-init flow:**
```
Install Docker → Open UFW ports → Download image → Start container (both ports) →
Wait for "gateway ready" in logs → [API key injection - NOT YET] → 
[Set origins - NOT YET] → Get IP → Call webhook → Done
```

**TESTED & WORKING (2026-05-11 7:33 PM PDT):**
- ✅ Fresh signup → dashboard goes green automatically (~5 min)
- ✅ Cloud-init: Docker, UFW, image download, container start all working
- ✅ Health check uses `docker logs | grep ready` (fixed from curl)
- ✅ API key injection working in cloud-init
- ✅ Allowed origins injection working (fixed substring mismatch)
- ✅ Webhook fires automatically from cloud-init
- ✅ Dynamic nginx routing via agent-map.conf
- ✅ Webhook triggers nginx map regeneration
- ✅ Dashboard link updated to use `userid.agent.laverdi.tech/openclaw/canvas/`
- ✅ Wildcard SSL working for all subdomains

**All 3 automation fixes DEPLOYED (2026-05-11 7:39 PM PDT):**
1. ✅ Cloud-init extracts gateway token, sends in webhook
2. ✅ Webhook saves token to `api_key` column in instances table
3. ✅ Cloud-init runs auto-approve device pairing loop (every 30s for 10 min)
4. ✅ Built and restarted

**Token auto-send:** DEFERRED — escaping issues with $GATEWAY_TOKEN in JS template literal webhook curl. Token will need to be fetched separately (portal-side script or user enters manually for now).

**Origins root cause found:** allowedOrigins gets overwritten by OpenClaw on restart. 
**Solution:** `dangerouslyAllowHostHeaderOriginFallback=true` — acceptable since instances are behind our SSL proxy + token auth.

**UUID alignment:** Everything now uses full UUID (nginx map, dashboard links, cloud-init origins). No more substring(0,8) mismatches.

**Latest test (2026-05-11 11:25 PM):** Everything worked until gateway login — 502 because nginx map wasn't regenerated. Manual `node update-nginx-agents.js` fixes it.

## ALL ISSUES RESOLVED (2026-05-12 9:04 PM PDT)

**FULLY AUTOMATED PROVISIONING PIPELINE:**
1. ✅ User signs up → Vultr instance created
2. ✅ Cloud-init: Docker, UFW, image download, container start
3. ✅ Config pre-mounted as directory volume (`/opt/openclaw-config:/root/.openclaw/.openclaw`)
4. ✅ `dangerouslyDisableDeviceAuth: true` — no device pairing needed
5. ✅ `dangerouslyAllowHostHeaderOriginFallback: true` — no origin errors
6. ✅ API key injected via docker exec after boot
7. ✅ Token extracted via node `String.fromCharCode` trick (avoids all escaping)
8. ✅ Token sent to portal via `/api/update-token` GET endpoint
9. ✅ Token saved to `api_key` column in instances table
10. ✅ Dashboard link includes `#token=` in URL fragment (auto-auth)
11. ✅ Webhook fires → nginx map regenerated → `systemctl reload nginx`
12. ✅ User clicks "Open Web Interface" → connected to their AI assistant

**Key architecture decisions:**
- Config as directory mount (not file mount — avoids EBUSY)
- `dangerouslyDisableDeviceAuth` acceptable because instances are behind SSL proxy + token auth
- Token via GET `/api/update-token` (avoids JSON escaping in cloud-init)
- Token in URL fragment `#token=` (not query string — more secure per docs)
- Full UUID everywhere (nginx map, dashboard, origins)

**Final bug fixed (2026-05-12 9:49 PM):** update-token.ts had missing `||` operators in Supabase client init.

**FULLY VERIFIED ZERO-TOUCH (2026-05-12 10:36 PM):**
- Fresh signup → dashboard green → click "Open Web Interface" → auto-authenticated → **IN THE OPENCLAW DASHBOARD**
- Token auto-saves via `/api/update-token` GET endpoint
- Token auto-fills via `#token=` URL fragment  
- No manual token entry, no device pairing, no origin errors
- Model: `openai/llama3.3-70b-instruct` via Vultr inference API
- **THIS IS THE WORKING PRODUCT.**

**Remaining polish (not blocking):**
- Nginx map sometimes needs manual `node update-nginx-agents.js` (timing issue)
- Double-provision guard in place but may need testing
- Cleanup old Vultr instances after failed tests
- All `||` operators in TypeScript files on server may get corrupted by SCP — always verify after transfer

## NEXT PRIORITIES (in order)

### 1. Inference Setup ✅ DONE
- Custom provider `vultr` configured in openclaw.json via cloud-init
- API: `openai-completions` compatible, baseUrl: `https://inference.do-ai.run/v1`
- **Default model: `vultr/deepseek-v4-pro`** (good speed + reliability)
- Anthropic/OpenAI models BLOCKED on current Vultr tier (403 forbidden)
- Available models configured: deepseek-v4-pro, llama3.3-70b, llama-4-maverick, deepseek-3.2, gemma-4-31B, mistral-3-14B, qwen3-32b
- Users can switch with `/model vultr/model-name` in chat
- Mistral 14B is fastest but breaks on tool calls (message ordering)
- Llama 70B is most reliable but slower

### Admin Panel
- Update script written: `admin-panel-update.py` (adds Instance IP, Model columns, updated pricing)
- Admin password change to `SandyGirl75!` — NOT YET APPLIED (needs sed + rebuild on portal)
- Password is in `NEXT_PUBLIC_ADMIN_PASSWORD` in `.env.local` — requires rebuild (baked at build time)

### 2. Startup Script / Update Bypass (MEDIUM)
- OpenClaw auto-updates can break containers
- Need: Pin version in Docker image OR disable auto-update in config
- Need: Update approval system (Chris approves before rollout)
- Config option: `gateway.autoUpdate: false` or similar

### 3. Onboarding Tutorial (LAST)
- First-time user guide in the Control UI
- Welcome message / prompts
- "Here's what your AI can do" tutorial
- Only after inference + updates are stable

---

## Database State (2026-05-11 4:02 PM PDT)
- All users wiped (clean slate)
- All instances wiped
- All Vultr instances deleted (except portal + fife-rv)
- Ready for fresh test

## Vultr Instances (Current)
- `laverdi-portal-seattle` (66.42.70.66) — Portal server
- `fife-rv-receptionist` (45.76.242.66) — Separate project
- All openclaw-* instances deleted

## Session Duration
- Started: ~4:50 PM PDT (May 8)
- MVP verified: ~6:34 PM PDT
- All fixes applied: ~7:31 PM PDT
- Full product loop: ~12:13 PM PDT (May 11)
- Cloud-init automation fixes: ~4:00 PM PDT (May 11)
- Major breakthrough after days of debugging
