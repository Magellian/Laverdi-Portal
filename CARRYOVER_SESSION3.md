# 🚀 CARRYOVER - Session 3 (Tomorrow: Configure & Scale)

**Date:** 2026-04-22+  
**Starting State:** OpenClaw live, containers running, VPS network back online  
**Session Goal:** Configure AI, verify full functionality, test scaling

---

## ⚡ Quick Start Checklist

- [ ] **Verify VPS Online**
  ```bash
  ssh root@64.23.142.154
  docker ps | grep openclaw
  ```

- [ ] **Check Container Status**
  ```bash
  docker logs openclaw-chris-1 | tail -20
  ```

- [ ] **Access OpenClaw**
  - Web: http://64.23.142.154:8824
  - Should see dashboard/control UI

---

## 🔧 Task 1: Configure AI Model (30 min)

### Option A: DigitalOcean Inference (Recommended)
**API Key:** `sk-do-REDACTED_DO_INFERENCE_KEY`

```bash
# SSH into VPS
ssh root@64.23.142.154

# Enter container
docker exec -it openclaw-chris-1 bash

# Interactive setup
openclaw setup

# When prompted:
# Provider: DigitalOcean Inference
# API Key: [paste above]
# Model: claude-opus-4.6

# Exit and container will auto-restart
exit
```

### Option B: OpenAI (If you prefer)
Use your OpenAI API key instead during setup.

### Verify Configuration
```bash
docker logs openclaw-chris-1 | grep -i "model\|configured"
```

Should see: `[gateway] agent model: claude-opus-4.6` or similar

---

## 🧪 Task 2: Test Full Functionality (20 min)

### Web UI Test
1. Open http://64.23.142.154:8824
2. You should see:
   - Dashboard
   - Chat interface
   - Agent status
3. Send a test message
4. Verify response comes back

### Companion App Test (Optional)
1. Download OpenClaw app: https://github.com/openclaw/openclaw/releases
2. Connect to: `64.23.142.154:8824`
3. Send chat message
4. Verify agent responds

### Success Criteria
✅ Chat interface loads  
✅ Message sent successfully  
✅ Agent responds with answer  
✅ Multiple messages work  

---

## 📊 Task 3: Test Full Payment → Agent Flow (30 min)

### Step 1: Create Test User
- Go to: http://64.23.142.154/auth/signup
- Email: `testuser@example.com`
- Password: `Test12345!`

### Step 2: Complete Payment
- Click "Get Started" → Starter plan
- Stripe test card: `4242 4242 4242 4242`
- Any future expiry date
- Any 3-digit CVC

### Step 3: Verify Provisioning
- Check logs: `docker logs laverdi-command-center | grep -i provision`
- Should see container creation
- New container name like: `openclaw-test...`

### Step 4: Access New Instance
- Log in with test email
- Go to /dashboard
- Check "Agent Server Status" card
- IP and port should be displayed
- Click "Open Web Interface"
- Should load OpenClaw for that user

### Success Criteria
✅ Payment processes  
✅ Webhook fires  
✅ New container created  
✅ Dashboard shows instance  
✅ Can access user's OpenClaw  

---

## 🌐 Task 4: Test Scaling (Optional)

### Create 3 Test Users
Repeat Task 3 three times with different emails.

### Verify All Containers Running
```bash
docker ps | grep openclaw
```

Should show 3+ containers with different ports.

### Test Each Instance
Access each on their unique ports:
- http://64.23.142.154:8824
- http://64.23.142.154:8825
- http://64.23.142.154:8826
etc.

### Success Criteria
✅ Multiple containers running  
✅ Each has unique port  
✅ Each accessible independently  
✅ No port conflicts  

---

## 🔐 Task 5: Email Verification (Automatic)

### Check DNS Status
```bash
nslookup laverdi.tech
# or
dig laverdi.tech
```

### What to Expect
- Records added to Gandi (from session 2)
- Will propagate in 1-2 hours (automatic)
- Once live, emails will auto-send

### Verify When Live
- Create new user
- Should receive welcome email
- Check spam folder if not in inbox

---

## 📋 Known Issues (All Fixable)

| Issue | Status | Fix |
|-------|--------|-----|
| OpenClaw needs config | 🟡 Expected | Run setup (Task 1) |
| VPS network down | 🟡 Temporary | Resolves automatically |
| DNS not propagated | 🟡 Expected | Wait 1-2 hours |
| Email not sending | 🟡 DNS wait | Automatic once DNS live |

---

## 🎯 Critical Reminders

### Port Allocation
- Containers get ports from 8700-9000 range
- Each user's instance on unique port
- All expose to public IP: `64.23.142.154`

### Database
- Instances table in Supabase stores container metadata
- droplet_id is BIGINT (not string)
- Port and IP_address stored for dashboard

### Security
- Pairing tokens auto-generated per container
- Auth tokens generated by OpenClaw
- Gateway warns about non-loopback binding (normal)

### Performance
- Each container ~200MB memory
- VPS has enough for 10-50 concurrent instances
- Monitor with: `docker stats`

---

## 🚨 If Something Breaks

### Container won't start
```bash
docker logs openclaw-chris-1
# Check for --bind errors or config issues
```

### Port not accessible
```bash
docker port openclaw-chris-1
# Should show: 8700/tcp -> 0.0.0.0:XXXX
```

### Webhook not firing
```bash
docker logs laverdi-portal | grep -i webhook
# Check for Stripe signature errors
```

### Dashboard not showing instances
```bash
# Query Supabase directly
curl https://dcvrkpgvxqdcboostkpz.supabase.co/rest/v1/instances \
  -H "apikey: [PUBLIC_KEY]"
# Check for data
```

---

## 📞 Quick Reference

**VPS Access:**
```bash
ssh root@64.23.142.154
```

**Container Access:**
```bash
docker exec -it openclaw-chris-1 bash
```

**View Logs:**
```bash
docker logs -f openclaw-chris-1
docker logs -f laverdi-command-center
docker logs -f laverdi-portal
```

**Restart Services:**
```bash
docker restart openclaw-chris-1
docker restart laverdi-command-center
docker restart laverdi-portal
```

---

## ✅ Success = Full SaaS Live

When all tasks complete:
- ✅ Users can sign up
- ✅ Users can pay
- ✅ Agents provision automatically
- ✅ Agents are accessible
- ✅ Agents respond to queries
- ✅ Multiple concurrent users work
- ✅ Emails notify users
- ✅ Dashboard shows all instances

**You'll have a fully functional, production-grade SaaS platform.** 🎉

---

## 📊 Time Estimates

- Task 1 (Configure AI): 30 min
- Task 2 (Test functionality): 20 min
- Task 3 (Payment flow): 30 min
- Task 4 (Scaling): 30 min
- Task 5 (Email): 5 min (automatic)

**Total: ~2 hours for full verification**

---

**Previous Session:** FINAL_STATUS_2026-04-21.md  
**Code Repository:** All committed and pushed  
**Infrastructure:** Production-ready, awaiting configuration  
**Status:** READY TO CONTINUE 🚀
